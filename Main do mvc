public class Main {
    public static void main(String[] args) {
        AgendaTelaView view = new AgendaTelaView();
        AgendaController controller = new AgendaController(view);
        view.showView();
    }
}
